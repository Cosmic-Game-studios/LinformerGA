<x-app-layout>
    {{-- Seitenleiste: Log-Dateien --}}
    <x-app-marginal class="bg-body border-end">
        <div class="list-group list-group-flush">
            @foreach($files as $file)
                @php $active = $currentFile === $file->getFilename(); @endphp
                <a href="{{ route('admin.errorlog.index', ['file' => $file->getFilename()]) }}"
                   class="list-group-item list-group-item-action d-flex justify-content-between
                          align-items-center border-0 py-3 {{ $active ? 'active bg-primary text-white' : 'sidebar-item' }}">
                    <span><i class="fas fa-file-alt me-2 text-body-secondary"></i>{{ $file->getFilename() }}</span>
                    <small class="badge bg-body-secondary text-body rounded-pill">{{ number_format($file->getSize()/1024,1) }} KB</small>
                </a>
            @endforeach
        </div>
    </x-app-marginal>

```
{{-- Hauptbereich --}}
<x-app-content>
    <div class="card border-0 shadow-sm mb-4">
        <div class="card-header bg-body border-0 d-flex justify-content-between align-items-center py-3">
            <h5 class="mb-0 text-body-emphasis"><i class="fas fa-bug me-2 text-danger"></i>{{ $currentFile }}</h5>

            <div class="btn-group">
                {{-- Download --}}
                <a href="{{ route('admin.errorlog.download', ['file' => $currentFile]) }}"
                   class="btn btn-sm btn-body-secondary border-0 shadow-sm">
                    <i class="fas fa-download text-body-secondary"></i>
                </a>

                {{-- Clear --}}
                <form action="{{ route('admin.errorlog.clear') }}" method="POST"
                      onsubmit="return confirm('Alle älteren Log-Dateien löschen?');">
                    @csrf @method('DELETE')
                    <button type="submit" class="btn btn-sm btn-body-secondary border-0 shadow-sm text-danger">
                        <i class="fas fa-trash-alt"></i>
                    </button>
                </form>
            </div>
        </div>

        <div class="card-body p-0 bg-body-tertiary" style="max-height:70vh; overflow:auto">
            <pre class="m-0 px-4 py-3 text-body bg-body-tertiary" style="font-family: 'Fira Code', 'Consolas', monospace; font-size: 0.875rem; line-height: 1.5;">
```

@foreach($logs as $entry)
<span class="
@switch($entry['level'])
@case('error')   text-danger fw-bold @break
@case('warning') text-warning fw-bold @break
@case('critical') text-danger fw-bold bg-danger bg-opacity-25 px-1 rounded @break
@default text-body
@endswitch
">{{ $entry->raw }}</span>
@endforeach
</pre>
</div>

```
        <div class="card-footer bg-body border-0 d-flex justify-content-center py-3">
            {{ $logs->withQueryString()->links() }}
        </div>
    </div>
</x-app-content>
```

</x-app-layout>

@push(‘scripts’)

<script>
document.addEventListener('DOMContentLoaded', () => {
    const box = document.querySelector('.card-body');
    if (box) box.scrollTop = 0;
});
</script>

@endpush

<style>
.sidebar-item {
    background-color: var(--bs-body-bg);
    color: var(--bs-body-color);
}

.sidebar-item:hover {
    background-color: var(--bs-secondary-bg) !important;
    color: var(--bs-body-color);
}

.list-group-item.active {
    background-color: #0d6efd !important;
    border-color: #0d6efd !important;
}

.card {
    border-radius: 0.5rem;
    background-color: var(--bs-body-bg);
}

.card-header {
    border-radius: 0.5rem 0.5rem 0 0 !important;
}

.card-footer {
    border-radius: 0 0 0.5rem 0.5rem !important;
}

.btn-group .btn {
    border-radius: 0.375rem !important;
    margin-left: 0.25rem;
}

.btn-group .btn:first-child {
    margin-left: 0;
}

.btn-body-secondary {
    background-color: var(--bs-secondary-bg);
    color: var(--bs-body-color);
}

.btn-body-secondary:hover {
    background-color: var(--bs-tertiary-bg);
    color: var(--bs-body-color);
}

pre {
    border-radius: 0 !important;
}
</style>