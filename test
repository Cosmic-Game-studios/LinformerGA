<x-app-layout>
    {{-- Seitenleiste: Log-Dateien --}}
    <x-app-marginal class="bg-white border-end border-light">
        <div class="p-4">
            <h6 class="text-muted text-uppercase fw-semibold mb-3 small">Log-Dateien</h6>
            <div class="d-flex flex-column gap-1">
                @foreach($files as $file)
                    @php $active = $currentFile === $file->getFilename(); @endphp
                    <a href="{{ route('admin.errorlog.index', ['file' => $file->getFilename()]) }}"
                       class="d-flex justify-content-between align-items-center p-3 rounded-2 text-decoration-none
                              {{ $active ? 'bg-primary text-white shadow-sm' : 'text-dark hover-bg-light' }}
                              transition-all">
                        <div class="d-flex align-items-center">
                            <i class="fas fa-file-alt me-3 {{ $active ? 'text-white' : 'text-muted' }}"></i>
                            <span class="fw-medium">{{ $file->getFilename() }}</span>
                        </div>
                        <small class="opacity-75">{{ number_format($file->getSize()/1024,1) }} KB</small>
                    </a>
                @endforeach
            </div>
        </div>
    </x-app-marginal>

```
{{-- Hauptbereich --}}
<x-app-content>
    <div class="container-fluid px-4 py-4">
        <div class="row">
            <div class="col-12">
                {{-- Header --}}
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <div>
                        <h4 class="mb-1 fw-semibold">Error Logs</h4>
                        <p class="text-muted mb-0">{{ $currentFile }}</p>
                    </div>
                    
                    <div class="d-flex gap-2">
                        {{-- Download --}}
                        <a href="{{ route('admin.errorlog.download', ['file' => $currentFile]) }}"
                           class="btn btn-outline-secondary btn-sm d-flex align-items-center gap-2">
                            <i class="fas fa-download"></i>
                            <span>Download</span>
                        </a>

                        {{-- Clear --}}
                        <form action="{{ route('admin.errorlog.clear') }}" method="POST"
                              onsubmit="return confirm('Sind Sie sicher, dass Sie alle älteren Log-Dateien löschen möchten?');">
                            @csrf @method('DELETE')
                            <button type="submit" class="btn btn-outline-danger btn-sm d-flex align-items-center gap-2">
                                <i class="fas fa-trash-alt"></i>
                                <span>Löschen</span>
                            </button>
                        </form>
                    </div>
                </div>

                {{-- Log Content --}}
                <div class="card border-0 shadow-sm">
                    <div class="card-body p-0">
                        <div class="bg-dark rounded-3 position-relative" style="max-height:70vh; overflow:auto">
                            <pre class="m-0 p-4 text-light font-monospace small lh-sm">@foreach($logs as $entry)<span class="
```

@switch($entry[‘level’])
@case(‘error’)   text-danger fw-bold @break
@case(‘warning’) text-warning fw-bold @break
@case(‘critical’) text-danger fw-bold bg-danger bg-opacity-25 px-1 rounded @break
@default text-light
@endswitch
“>{{ $entry[‘raw’] }}</span>
@endforeach</pre>
</div>
</div>

```
                    {{-- Pagination --}}
                    @if($logs->hasPages())
                    <div class="card-footer bg-light border-0 d-flex justify-content-center py-3">
                        {{ $logs->withQueryString()->links() }}
                    </div>
                    @endif
                </div>
            </div>
        </div>
    </div>
</x-app-content>
```

</x-app-layout>

@push(‘styles’)

<style>
.hover-bg-light:hover {
    background-color: #f8f9fa !important;
}

.transition-all {
    transition: all 0.2s ease-in-out;
}

.font-monospace {
    font-family: 'SF Mono', Monaco, 'Cascadia Code', 'Roboto Mono', Consolas, 'Courier New', monospace;
}

/* Scrollbar Styling */
.card-body div::-webkit-scrollbar {
    width: 8px;
}

.card-body div::-webkit-scrollbar-track {
    background: #2d3748;
    border-radius: 4px;
}

.card-body div::-webkit-scrollbar-thumb {
    background: #4a5568;
    border-radius: 4px;
}

.card-body div::-webkit-scrollbar-thumb:hover {
    background: #718096;
}

/* Log Level Indicators */
.text-danger {
    color: #dc3545 !important;
}

.text-warning {
    color: #ffc107 !important;
}

.text-info {
    color: #0dcaf0 !important;
}

.text-secondary {
    color: #6c757d !important;
}
</style>

@endpush

@push(‘scripts’)

<script>
document.addEventListener('DOMContentLoaded', () => {
    const logContainer = document.querySelector('.card-body div');
    if (logContainer) {
        logContainer.scrollTop = 0;
    }
});
</script>

@endpush